{% extends 'base.html.twig' %}

{% block title %}DeliveryAdresse{% endblock %}

{% block body %}
    <h1>DeliveryAdresse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delivery_adresse.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ delivery_adresse.City }}</td>
            </tr>
            <tr>
                <th>Postal_code</th>
                <td>{{ delivery_adresse.postalCode }}</td>
            </tr>
            <tr>
                <th>Full_name</th>
                <td>{{ delivery_adresse.fullName }}</td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ delivery_adresse.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_delivery_adresse_index') }}">back to list</a>

    <a href="{{ path('app_delivery_adresse_edit', {'id': delivery_adresse.id}) }}">edit</a>

    {{ include('delivery_adresse/_delete_form.html.twig') }}
{% endblock %}
