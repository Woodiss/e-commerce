<form method="post" action="{{ path('app_delivery_adresse_delete', {'id': delivery_adresse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_adresse.id) }}">
    <button class="btn">Delete</button>
</form>
